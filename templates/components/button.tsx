const buttonCode = `
<div className="flex flex-col items-center gap-4">
  {/* Try changing the variant to 'destructive' or 'outline' */}
  <Button variant="primary" theme="organic" size="default">
    Primary Button
  </Button>
 
</div>
`.trim();

export default buttonCode;